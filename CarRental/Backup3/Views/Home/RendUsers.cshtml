@model IEnumerable<CarRental.BLL.DTO.UserDTO>
@Styles.Render("~/Content/themes/base/all.css")
<div id="user_area">
    <p>
        <a href="@Url.Action("Register" )" class="btn btn-info ml-1"><i class="fa fa-plus mr-1"></i>Add Manager</a>
    </p>

    <table class="table table-bordered table-striped">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>

           
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <p>@item.Email</p>
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>

              
                <td>
                    @Html.ActionLink("Change", "EditUser", new { id = item.Id, area = "Admin" }, new { @class = "btn btn-warning" })
                    @Html.ActionLink("Detail", "UserDetails", new { id = item.Id, area = "Admin" }, new { @class = "btn btn-info" })

                </td>
            </tr>
        }

    </table>
</div>
<script>
    $(function () {
        $("[data-autocomplete-source]").each(function () {
            var target = $(this);
            target.autocomplete({ source: target.attr("data-autocomplete-source") });
        });
        $(document).on('change', ':input', function () {
            $("#sbm").click();
        });
    });
</script>